#include <iostream>
#include <math.h>
#include <string>
using namespace std;
int main()
{
	int k;
	string s;
	getline(cin, s);
	k = 0;
	for (int i = 0; i < s.length(); i++)
	{
		if ((s[i] >= 'A') && (s[i] <= 'Z'))
			k++;
	}
	cout << k;
}
