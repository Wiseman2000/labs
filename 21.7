#include <iostream>
#include <string>
#include <clocale>
#include <Windows.h>
using namespace std;
int main()
{
	char b;
	int k,r;
	int a;
	string s;
	string s0;
	setlocale(LC_ALL, "Russian");
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
        getline(cin, s);
		k = 0;
		r = 0;
		a = 0;
		for (int i = 0;i < s.length();i++)
		{
			if (i % 2 == 1)
				cout << s[i];
		}
			for (int i = s.length()-1;i >= 0;i--)
			{
				if (i % 2 == 0) cout << s[i];
			}
}
