#include <iostream>

using namespace std;



int main()

{
	int par, k, s, N, m;
	int a[1000];
	int b[1000];
	cin >> N;
	for (int i = 1;i <= N;i++)
	{
		cin >> a[i];
	}
	s = 1;
	k = 0;
		for (int i = 1;i <= N;i++)
		{
			par = 1;
			for (int j=1;j<=N;j++)
			if ((a[j] == a[i])&&(j!=i))
			{
				par++;
			}
			if (par != 2)
			{
				b[s] = a[i];
				s++;
				k = k + 1;
			}
		}
	for (int i = 1;i <= k;i++)
	{
		cout << b[i] << " ";
	}
    }
