#include <iostream>
#include <string>
#include <clocale>
#include <Windows.h>
using namespace std;
int main()
{
	char b;
	int k,r;
	bool a;
	string s;
	string s0 = "уеоыаэяиёюЁУЕОАЫЭЯИЮ";
	setlocale(LC_ALL, "Russian");
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
        getline(cin, s);
		k = 0;
		for (int i = 0;i < s.length();i++)
		{
			if (s[i] == '/') k++;
			if (s[i] == '.') k++;
			if ((k == 2) && (s[i] != '/')) cout << s[i];
		}
}
