#include <iostream>
#include <math.h>
using namespace std;
int Sign(double X)
{
	if (X < 0)
	{
		X = -1;
	}
	if (X == 0)
	{
		X = 0;
	}
	if (X > 0)
	{
		X = 1;
	}
	return X;
}


int main()

{
	double A,B,sum;
	cin >> A >> B;
	sum = Sign(A) + Sign(B);
	cout << sum << endl;
}
