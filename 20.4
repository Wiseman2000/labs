#include <iostream>
#include <math.h>
#include <string>
using namespace std;
int main()
{
	string s;
	char c;
        getline(cin, s);
		cin >> c;
		for (int i = s.length() - 1; i >= 0; i--)
		{
			if (s[i] == c) s.insert(i, 1, c);
		}
		cout << s << endl;
}
