#include <iostream>
#include <string>
#include <clocale>
#include <Windows.h>
using namespace std;
int main()
{
	char b;
	int k,r;
	int a;
	string s;
	string s0;
	setlocale(LC_ALL, "Russian");
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
        getline(cin, s);
		k = 0;
		r = 0;
		a = 0;
		for (int i = 0;i < s.length();i++)
		{
			if (s[i] == '/')
				r++;
		}
		if (r != 1)
		{
			for (int i = 0;i < s.length();i++)
			{
				if (s[i] == '.') a++;
				if (s[i] == '/')
					k++;
				if ((k == r - 1)&&(s[i]!='/')) cout << s[i];
			}
		}
		else cout << 0;
}
