#include <iostream>
#include <math.h>
#include <string>
using namespace std;
int main()
{
	char c,c1,c2;
	int k;
	cin >> c;
	k = int(c);
	c1 = char(k - 1);
	c2 = char(k + 1);
	cout << c1 << " " << c2;
}
